{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", "public/**"],
      "env": []
    },
    "lint": {
      "outputs": [],
      "inputs": ["eslint.config.*", "src/**"]
    },
    "type:check": {
      "outputs": ["*.tsbuildinfo"]
    }
  }
}
